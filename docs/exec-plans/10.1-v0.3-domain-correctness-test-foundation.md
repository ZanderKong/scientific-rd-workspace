# Plan 10.1 Execution Log — v0.3 Domain Correctness & Test Foundation

Source plan: `/Users/kong/Downloads/10.1-v0.3-domain-correctness-test-foundation.md`

Target branch: `feat/plan10-workspace-shell-resource-settings`

## Baseline

- Baseline commit: `f99499c feat: complete v0.3 domain model cutover`
- The baseline commit is an ancestor of the target branch.
- Working tree was clean before implementation.
- Backend baseline: `TEST_DATABASE_URL=postgresql+psycopg://.../scientific_rd_test_plan10_1 uv run pytest -q` — 10 passed.
- Tests use isolated PostgreSQL; SQLite is not used.

## Execution status

- [x] Provenance authority and public relation protections
- [x] Process execution semantic validation
- [x] Transaction-safe Sample Record aggregate
- [x] Definition, Data, View, Claim revision correctness
- [x] Asset/storage correctness
- [x] Legacy migration correctness
- [x] ChangeSet and graph correctness
- [x] Sample Composer core workflow
- [x] Golden workflow and domain invariant tests
- [x] Documentation and handoff

## Delivered

- Added explicit PostgreSQL Alembic revisions `0011_v0_3_provenance_pins` and `0012_v0_3_schema_alignment`; `alembic check` reports no drift.
- Removed runtime `Base.metadata.create_all` from the v0.3 migration path. The 0006-to-head migration was exercised with legacy objects, data, relations and SampleExecution rows.
- Enforced canonical ProcessExecution binding kinds, project scope, one Data output producer, Sample Record membership, explicit DAG cycle checks, and View revision pinning.
- Materialized `subject` and `derived_from` only from execution provenance; generic create/update/delete routes reject system-managed relations.
- Made domain versions/revisions/representations immutable and made generic Research Object writes append ObjectRevision rows.
- Made Sample Record create/update atomic and identity-preserving; the Web composer now edits multiple ordered steps and preserves `execution_id`.
- Hardened uploads with temporary streaming, size/digest verification, opaque storage keys, and asset reference preflight on delete.
- Added backend invariant tests in `api/tests/test_v03_domain_correctness.py` and frontend model coverage for execution identity.

## Verification

- Backend: isolated PostgreSQL `pytest -q` — 17 passed.
- Backend lint/format: Ruff check passed.
- Migration: fresh PostgreSQL `alembic upgrade head` and `alembic check` passed; 0006-to-head legacy fixture passed and preserved `{"temperature":{"value":42}}` plus binding values.
- Frontend: TypeScript, oxlint, Vitest — 12 passed, and `next build` passed.
- `npm run format:check` still reports pre-existing formatting differences in unrelated workspace files; all Plan 10.1 touched frontend files were formatted.

Plan 10.2 remains deferred.
