# Plan 10.2 Execution Log — v0.3 Verification, Browser E2E & Release Gate

Source plan: `/Users/kong/Downloads/10.2-v0.3-verification-browser-e2e-release-gate.md`

Target branch: `feat/plan10-workspace-shell-resource-settings`

## Baseline and scope

- Plan 10.1 implementation baseline: `03b17eb825caf9f142291f0f946be2ced49f3000`.
- The release gate keeps the v0.3 canonical kinds and does not restore any v0.2 runtime tables, payloads or resource kinds.
- PostgreSQL 17 is the only database used by tests, migrations and browser acceptance.

## Completed work

- [x] Added regression coverage for Sample Record update scope inheritance, retained execution identity, new steps and refreshed binding values.
- [x] Completed the Composer `/` definition resolver and `@` object/tag resolver. Definition versions are pinned; tag-derived defaults for material, equipment and substrate remain editable.
- [x] Rendered binding field definitions and preserved value/unit pairs separately from Process Execution fields.
- [x] Added stable `data-testid` selectors to the Composer and Project creation flow.
- [x] Added a repeat-safe `GoldenCl2WorkflowFactory`, seed verifier, canonical graph benchmark and active legacy-runtime scan.
- [x] Added fresh-head and 0006-to-head PostgreSQL migration verification. The legacy test inserts concrete old rows before the v0.3 cutover and verifies canonical kinds/tags and removed legacy tables.
- [x] Removed the Alembic cycle warning by marking current View/Claim revision constraints as deferred DDL dependencies; `alembic check` reports no drift without the prior warning.
- [x] Added true MCP stdio and Streamable HTTP contract tests using `mcp.ClientSession`, including REST/MCP capability and Project Context parity.
- [x] Replaced shell/CLI browser smoke scripts with `@playwright/test` suites, trace/video/screenshot evidence and browser health guards.
- [x] Added first-run/no-seed, Composer aggregate and golden scientific workflow browser suites at 1440×900 and 1024×768.
- [x] Added View and Claim domain detail surfaces, Data origin visibility, a reachable Research Object detail route and Experiment deletion semantics that preserve shared references.
- [x] Rebuilt CI as blocking Backend, Migration, Frontend, MCP, Browser First Run and Browser Golden Scientific Workflow gates. Runtime production dependencies are checked with `npm audit --omit=dev --audit-level=high`.
- [x] Reformatted the complete web workspace with the repository formatter and added format checks to CI.

## Local verification record

```text
api/
TEST_DATABASE_URL=.../scientific_rd_test_plan10_2 uv run pytest -q
21 passed

DATABASE_URL=.../scientific_rd_migration_plan10_2 uv run alembic upgrade head
DATABASE_URL=.../scientific_rd_migration_plan10_2 uv run alembic check
No new upgrade operations detected.

DATABASE_URL=.../scientific_rd_legacy_plan10_2 uv run alembic upgrade 0006_agent_changes
uv run python -m scripts.prepare_legacy_v02_fixture
uv run alembic upgrade head
uv run python -m scripts.verify_legacy_cutover
Legacy 0006 to v0.3 cutover verified

web/
npm run lint:strict
npm run typecheck
npm test -- --run
13 tests passed
npm run format:check
npm run build
npm audit --omit=dev --audit-level=high
found 0 vulnerabilities

npx playwright test e2e/first-run.spec.ts
1 passed
npx playwright test e2e/sample-composer.spec.ts
1 passed
npx playwright test e2e/golden-scientific-workflow.spec.ts
1 passed
```

## Release evidence

GitHub Actions is the final authority for release status. The pushed commit must have all six required jobs green before this plan is considered released:

- Backend / PostgreSQL 17
- Migration / Legacy 0006 → v0.3
- Frontend / Quality
- MCP / Contract
- Browser / First Run
- Browser / Golden Scientific Workflow
