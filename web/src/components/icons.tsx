import type { ComponentType } from 'react';
import { IconBrightness, type IconProps } from '@tabler/icons-react';

export type Icon = ComponentType<IconProps>;

export const Icons = {
  brightness: IconBrightness
};
